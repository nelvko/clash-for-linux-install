#!/usr/bin/env bash

script_home=$(cd -- "$(dirname -- "$(readlink -f "${BASH_SOURCE[0]}")")/.." && pwd -P)
export CLASHCTL_HOME="$script_home"

. "$CLASHCTL_HOME/scripts/runtime/command.sh"

clashctl "$@"
